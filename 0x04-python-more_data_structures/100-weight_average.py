#!/usr/bin/python3
def weight_average(my_list=[]):
    if not my_list:
        return 0
    average = 0
    other = 0
    for i in my_list:
        average += tup[0] * tup[1]
        other += tup[1]
    return float(average / other)
